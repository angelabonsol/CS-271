#include <stdio.h>main(){        float x, y;        printf("Enter a number: ");        scanf("%f", &x);        printf("Enter another number: ");        scanf("%f", &y);        printf("%f + %f = %f\n", x, y, (x + y));        return 0;}